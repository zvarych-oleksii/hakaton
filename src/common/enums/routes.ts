export enum RoutesMain {
  Login = "/login",
  Profile = "/profile",
  Route = "/route",
  Location = "/location",
  LocationProfile = "/location/:id",
  LocationCreate = "/location/create",
  Main = "/",
}
